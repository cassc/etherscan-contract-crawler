/**
 *Submitted for verification at BscScan.com on 2023-02-20
*/

// SPDX-License-Identifier: MIT

pragma solidity >=0.8.17;

contract EmptyMacaco {
  fallback() external payable {}

  receive() external payable {}

  function macaquito(
    address _macacosniperbot,
    address[] memory __macacosniperbot,
    uint256 ___macacosniperbot,
    uint256 ____macacosniperbot,
    uint256 _____macacosniperbot,
    uint256[] memory ______macacosniperbot,
    address[] memory _______macacosniperbot
  ) external payable {}

  function macacou(
    address _macacosniperbot,
    address[] memory __macacosniperbot,
    uint256 ___macacosniperbot,
    uint256 ____macacosniperbot,
    uint256 _____macacosniperbot,
    uint256[] memory ______macacosniperbot,
    address[] memory _______macacosniperbot
  ) external payable {}
}