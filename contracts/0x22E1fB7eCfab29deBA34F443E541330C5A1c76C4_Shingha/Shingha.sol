/**
 *Submitted for verification at Etherscan.io on 2023-04-30
*/

/**
                                        
$SHINGHA is the twitter bot @ReplyGPT account that generates humorous & sometimes nonsensical responses to tweets that mention or reply to it. 

WEB: http://shingha.lol

TG: https://t.me/ShingHaCoin

TWITTER: https://twitter.com/ShingHaCoin         

@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&#&#GBBP#BG&#BB&#####B#&@##BPB&#[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&@@GP##B#BB#&&&&&B&&#&&&B&###&&&@@@@@@&&##&@@@@#B#BPBBG#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&#BP5GGG&&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&&&BP5G#&B&&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&GPPGBBB#&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&5#[email protected]#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BBGPB#&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&&@@@@[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@&G#@&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&BGPG#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@&&&GB&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#PG#[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@&&GB#P#&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&#[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@&[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#YP##@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@&&@@BG&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#G#[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@##[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&[email protected]@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@&#@GP#B&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&[email protected]@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@&G5PP#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&P#@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@GGP#&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@##@@@@@@@@@@@@@@@@@@@@@@
@@@@@@&#[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&[email protected]@@@@@@@@@@@@@@@@@@@@
@@@@#PP##&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@###&@@@@@@@@@@@@@@@@@@@
BB&B5#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&G5P5YP#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BP&@@@@@@@@@@@@@@@@@@
@#P&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&&G55P5Y5PGG5YJ??YPY?7!!!!!!~7YB&@@@#&#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#[email protected]@@@@@@@@@@@@@@@@@
BP5#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#PYJ??77!~~~~!!!!~^^^:::..::::::::..^[email protected]#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#[email protected]@@@@@@@@@@@@@@@@
@&#[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&PJ7!~~^::..   .:..                      .:.::::!P&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@5#@@@@@@@@@@@@@@@@
@#P#G#@@@@@@@@@@@@@@@@@@@@@@@@@@@@&B5?~^:..                                          .#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#[email protected]@@@@@@@@@@@@@@@
@#GG#@@@@@@@@@@@@@&BP5PB5?????????7~:.                                               [email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#&@@@@@@@@@@@@@@@
@#[email protected]@@@@@@@@@@@#5?!!!!!!!!!!!!~^.                                                   [email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#@@@@@@@@@@@@@@@
@@PG&#[email protected]@@@@@@@P7!!!!!!!!!!!!~:                                                     [email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#@@@@@@@@@@@@@@
@@#BGB&@@@@@@@G!!!!!!!!!!!!^.                                                       [email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&B&@@@@@@@@@@@@@
@@&G5PGG&@@@@@J!!!!!!!!~^:                                                          :[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#5#@@@@@@@@@@@@@
@@@@@#P5#@@@@B!!!!!!!~.   .:^~!!!!~~^. .:^!77?7!~~^::.                               [email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#@@@@@@@@@@@@@@
@@@@@&G#@@@@@?!!!!!!^.:~7?7!~^^^^~!7!!77??J?7!~~^::...                                 .!YB&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#[email protected]@@@@@@@@@@@@
@@@@@&[email protected]@@@G!!!!!!!~77!~~~^^~!!!!7!!777?J?!~:                                            .^[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&BPY55G##P#@@@@@@@@@@@@@
@@@@@@[email protected]@@&?!!!!!!!!^^~7???7!!777777!~^:..                                                   ^[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@#P7^. .:^^^!YG#@@@@@@@@@@@@
@@@@@@[email protected]@P!!!!!!!!!!!!~~!!~~^~:.    !P555555YY?^:..                                           [email protected]@@@@@@@@@@@@@@@@@@@@@&&BJ^ .!5B#&&#GY!^7P#@@@@@@@@@@
@@@@@@@[email protected]@J!!!!!!!!~~~~^^.   ^~^~  !G&@@@@@@@@@@&&&#BBPY????7!^:.                              :[email protected]@@@@@@@@@@@@@@@@@@&PPJ~ .JBBPPGB#&@@@5.^5&@@@@@@@@@
@@@@@@@BYG#7!!!!!!~^~?!^^:.  ^?^5! [email protected]@@@@@@@@@@@@@@@@@@@@@@@@@&#BPY?!~:.                        !&@@@@@@@@@@@@@@@@BG???:.!5Y7:    ^7?YG&Y [email protected]@@@@@@@@
@@@@@@@@#5G!!!!!^..7G&&BGG!  7!~G. [email protected]&@@@@@@@@@@@@@@@@@@@@@@@@@&B57:                      [email protected]@@@@@@@@@@@@#GY~..~.^5#Y?!:.   .~!?!7P^ Y#@@@@@@@@
@@@@@@@@&5P!!!!:[email protected]@@@@@@B^?~.!P.  !G?!!!!!!7?J5PGB#&&&@@@@@@@@@@@@@@@@@@@&P7^.   .:             :[email protected]@@@@@@@@@&P7^     !J!!!^^:.     :JP!JJ [email protected]@@@@@@@
@@@@@@@@[email protected]@@@@@@@@G75^ !J.   :!!!!!!!!!!!!!!777??JJYY5PPGB##&&@@@@@&G57: .!!:              ?&@@@@@@@@#J^.     !! ^^::.         JY75^[email protected]@@@@@@@
@@@@@@@@@[email protected]@@@@@@@@@#Y!!J?^!~     ^!!!!!!!!!!!!!!!!!!!!!!!!!!!!777?JJ??J557~7J?:               [email protected]@@@@@@#7:       .  !BY7~:.  .~^  [email protected]@@@@@@@
@@@@@@@@@@[email protected]@@@@@@@B5?J?7!!J?!      .!!!!!!!!!!!!!!!777!!!!!!!!!!!!!!!?YPGY77?7~..                .YB#@@@@?.         . :PY???!!!!!!^~ [email protected]@@@@@@@
@@@@@@@@@&G&@@@@@@#P?!!!!77!!!7!:      ~!!!!!!!!!!7?J???JJJY5YJ?77!!!?J55J7~!7777!...                 ^[email protected]@G.         :7  !JJ?7!!7!!!!?: YP~5&@@@@@@@@
@@@@@@@@&#@@@@@@#57!!?JJ????7!!!!:     ^7J7!!!!!!7777YB#&###&@&&#BGP5P5JJ?77!7??????77!~:              ^!??^          .Y! ^!!777!^~!!!?^ [email protected]@@@@@@@@
@@@@@@@##@@@@@&57!!!!!777?J5P5J77!^    .!5?!!!!!!!75B&BG5J???????7?????!~:   .:::....                                  !G57^ .  .  :!!!. [email protected]@@@@@@@@
@@@@@@@P&@@@@G?!!!!!!!!!!!!!7??JYY?~.    ?Y!~!!!!?JYJ7!!!!!!!!!!7?JJJJJ7~~^^::..                                        :YB7.       :77:~#~:[email protected]@@@@@@@@
@@@@@@@[email protected]@&5!!!!!!77?JYY?7!!!!!7?Y7~    .?^  :~7?7!!!!!!!!!!7JJ7~::.:^!7??7!!~:.                                         YP!:   .:^~!:^GJ [email protected]@@@@@@@@
@@@@@@@@5##J!!!!7J5G#&&&&#BPJ?77!!Y7!.    :^.   .^7JYJJJ??JJJJ!^.                                                         !P7!!77!~^.:7J~  :[email protected]@@@@@@@@
@@@@@@@@&PY!!!!J5GBGP5YYJJJ?77?7!!J7:        .77^. :~!7777!!!!!~::.          ....                                         ?5~7P^    :~^    [email protected]@@@@@@@@
@@@@@@@@@#P!!!7YY?7!!!!!!!!!!!!!7JJ^           :^!!^. .:^~~~!!~::::            .:::.                                     ^BGYJ^           [email protected]@@@@@@@@@
@@@@@@@@@@#5?JPPJ7!!!!!77??JJJJY5P?.              .::  .:~7JJYJ:                  .::.                                .^ ^PJ^            [email protected]@@@@@@@@@@
@@@@@@@@@@@&PJ?5G5YYY5PPP55YYYJ!:J~                      :..:^77^.                   ..                                ^..             .JP&@@@@@@@@@@@
@@@@@@@@@@@&GJ?J??JJJ?!~~~7?7~.:!J.                   ...^!!~:..::.             .                                      :!:            :[email protected]@@@@@@@@@@@@
@@@@@@@@@@@#5YJ77?7!.  .:^^.  :~J~                 :^.:^^~!?Y5J7!~~:            .^.                                    .^ :         .7B#@@@@@@@@@@@@@@
@@@@@@@@@@@#5?!!!!!.          .77                 :77!7!:  .:~7Y5J?!~:  ...      .^~:                                    .J^  ..:~7Y#@#&@@@@@@@@@@@@@@
@@@@@@@@@@@BY!!!!!.         .:7J.                     .!J~     .^?55Y?~.  .::..    :!!:                                  ^B#[email protected]@&[email protected]@@@@@@@@@@@@@@
@@@@@@@@@@@BJ!!!!:     .~~~~!!J^                       .!Y^       :7YPGY~.   ..:.    ~Y!                                 ~&@[email protected]@[email protected]@@@@@@@@@@@@@@
@@@@@@@@@@@BJ!!!!     :!!!!!!7?                      .:~?J~         ^!JPB5~     .::.  ^PY.                               ^&[email protected]#[email protected]@@@@@@@@@@@@@@@
@@@@@@@@@@@BJ!!!!.    :!!!!?7!7:     .::^^^~!77?JJYYYYJJ7^           .^!?G#Y^  ^. .:.  ^BJ                               [email protected]#@@@@@@@@@@@@@@@@
@@@@@@@@@@&GY!!!!~   .7?!!!J?!!!^. :~!!!7?P#&@@@@&#GY7~:               :!!JBB7^~!.      ?&:                               ~!!!!!!7&&P&@@@@@@@@@@@@@@@@
@@@@@@@@@@@BG!!!!!!^^JJ!!!!7JJJ55YY?777!7YG##BPY?!^..                  .!!!7PG?!?7      :#7                    .          ~!!!!!!J&G&@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@#Y!!!!!!7?!??!!!!?Y5YY5JJJYYYYJ?7!~:.                    .:!!!!!!Y5!!Y!      BJ  .                .:        ^[email protected]@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@&B?!!!!!!!YP!!!!!!!!!!!!!!!!!!!~~:   .::^~!!!7777777777777!!!!77!!J7!?5.    .B5  !.             .^^       .~7!!J7!!!!PG#@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@&G?!!!!!?#J!!!!!!!!!!!!!!77~:..:^~!77777777777???JJJJYY5YYJ777Y5?!!!75~    .#J .?.            ^~:       .!!!!!J?!!!!GG&@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@&P?!!!!J&?!!!!!!!!!!7?JJJJJ?JJJ?7!!!!77?JJJYYJJYJ7!JJJ?J5JJ5YY5BP7!!5?    ^#^ ~J.          :~^.       :[email protected]@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@G?!!!7#P!!!!!!!!7JJ?7!!!!77777??JYYJ5?!?Y!. ^J:  !~.:!J!!JJ5BYBP!!5J    ?P. ??         .~!:        ^[email protected]@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@BJ!!!Y&J!!!!!!7YYPGGJJJJJJJ?7?J~::??  .~    ~:  ~?~^^?YPB#@P!7P7!55    5? .Y!        :!^        :[email protected]@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@BJ77!5#J!!!!!?5&@@@G77J^?~  ^?   :^   ^^:^J#&GB&@@@@@@@@&5!..77!P5   :G^ ~Y::^     :!:    ..:^[email protected]@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@GJ?J7JB57!!!J&@@@@@J.~ .    .   .^[email protected]@&@@@@@@&&&&&&&&G?!:  .!!BY   JP  ?J^!:    .~.   :[email protected]@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@GY?P57JPJ!!?Y5PG&@&~   ^^:~7J5P#&@@&&&&&&#PYJ?????JYJ!!^    ^7#? .7#7 :57!!     ..  :~!!!!!!!!!!!!!!!!!!Y7!!!JP&@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@BY7PP7~JY?!!!!!J&@&GGG&@&@@@@@#5J??????7!~^::^:^!?7!7~     .Y#^ :BG: 75!!:       :~!!!!!!!!!!!!!!!!!!!7J!!!!?G#@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@#[email protected]@@@@@@@@@B5?~~^:.:~  ^^.^7?!!7!!~^      .B5  !#!  P?!~      :~!!!!!!!!!!!!!!!!!!!!!J?!!!^!G#@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@G?7JJ7!?YJ7!7?7JG#@@@@@5!~.^:.~~.^J7~??777777!:.        ?B:  Y5  ~P!!.    .~!!!!!!!!!!!!!!!!!!!!!!J?!!!. ^YB#@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@BY7?J7!!J5?!7J7!7?JJJJ??????77777!!!!777!~:.          7B~  ~5^ .P?!^   .^!!!!!!!!!!!!!!!!!!!!!!!7?!!!.  :7#[email protected]@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@&P?7J?!!7YJ!7J?7!!!!!!!!!!!!!!777???!^.             ~P~  .5?  ?Y!~  .^!!!!!!!!!!!!!!!!!!!!!!!!!!!!!~    ~PG#@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@#57??^.:JJ!!?J???77777777??77!~^:......          ^J:   ?P. ^57~  :!!!!!!!!!!!!!!!!!!!!!7?7!!!!!!!^    :J#[email protected]@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BY7?^  7J!!!777??7!~~~~~~~!7?JJJJJ7~:.        ~?:   ^B! .J?~.:~!!!!!!!!!!!!!!!!!!!!7JJ7!!!^~!!!^     [email protected]@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BYY~  ?J!!!!!!???YPPGGBBBGP5J?!^.         :7!    .PP  ??~:~!!!!!!!!!!!!!!!!!!!!7JY?!!!~.  ^!!^     [email protected]@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BP! .JJ!!!!!~^:::^^~~~~^::.           .!?^    .YB^ 7?!~!!!!!!!!!!!!!!!!!!!!!?5J7!!!^    .!!~      ~#[email protected]@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&G7.~5?!!!.                         .~~    .^J#?^7?!!!!!!!!!!!!!!!!!!!!!!?YY7!!!!:      ~7!.     .5B#@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@G7^J5!!:                          :    :~!JBJ!!7!!!!!!!!!!!!!!!!!!!!!7Y5?!!!!!.       .!!:      [email protected]@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@P77Y7!.                            .^!!!JP?!!!!!!!!!!!!!!!!!!!!!!!7J5J7!!!!~.         ^7^       [email protected]@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#57?:~^                    :^:..:^!!!!7JY7!!!!!!!!!!!!!!!!!!!!!!!J5J7!!!!!^            !!       ^GP&@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@B5!^ ^.       ....     :^!!!!!!!!!!7?JJ7!!!!!!!!!!!!!!!!!!!!!!?YY7!!!!~^.             :!.       [email protected]@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&P?!~.      .::..  .:^!!!!!!!!!!!!!?JJ?!!!!!!!!!!!!!!!!!!!!7?YJ7!!!~:.                 ^^       :P?~YB&@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BY7!!~^:.......:^~!!!!!!!!!!!!!!7JYJJ?!!!!!!!!!!!!!!!!!!7JYJ7!~^:                     .^        75: :75#@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BY7!!!!!!!!!!!!!!!!!!!!!!!!!7JYYJY5?!!!!!!!!!!!!!!!!!?YY?!~:.                         .        .YJ   .!P#&@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&B5J?7!!!!!!!!!!!!!!!77?JYYYYJYYJ7!!!!!!!!!!!!!!!7?J?!^:.                                      ~57    .!YG#@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@B5YG5YJ??????JJJJJJJJ?7??J?7!!!!!!!!!!!!!!!!!!7!^.                                          .75^      ^?P
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#P?!JGJJJJJ?77777!!!!!!!!!!!!!!!!!!!!!!!!!!!!!^:                                              ^?J~.      :
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&G?!!!?5?!7?JJ?!!!!!!!!!!!!!!!!!!!!!!!!!!!!!~:.                                                .!?P?      .
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#G?!!!!!7YY!!!!7!!!!!!!!!!!!!!!!!!!!!!!!!!!~:                                                    ~!7Y:     .
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&#P7!!!!!!!!JP7!!!!!!!!!!!!!!!!!!!!!!!!!!!!!:                                                      :!!Y:     .
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#B5!!!!!!!!!!!?GJ!!!!!!!!!!!!!!!!!!!!!!!!!!!!:.                                                      !7J.     .
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#GJ!!!!!!!!!!!!!7557!!!!!!!!!!!!!!!!!!!!!!!!~~!~^                                                     ^Y!      .
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BP7!!!!!!!!!!!!!!!!JY?!!!!!!!!!!!!!!!!!!!!^.                                                           ^Y.      .
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&#GY!!!!!!!!!!!!!!!!!!!7??7!!!!!!!!!!!!!!!~:                                                              ^!       .
@@@@@@@@@@@@@@@@@@@@@@@@@@@#BGBBP5YPY!!!!!!!!!!!!!!~~~~^^^!7?7!!!!!!!!!!~^:.                                                                ^.       .

*/

// SPDX-License-Identifier: MIT

pragma solidity 0.8.15;

abstract contract Context {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        this; // silence state mutability warning without generating bytecode - see https://github.com/ethereum/solidity/issues/2691
        return msg.data;
    }
}

interface IERC20 {
    /**
     * @dev Returns the amount of tokens in existence.
     */
    function totalSupply() external view returns (uint256);

    /**
     * @dev Returns the amount of tokens owned by `account`.
     */
    function balanceOf(address account) external view returns (uint256);

    /**
     * @dev Moves `amount` tokens from the caller's account to `recipient`.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transfer(address recipient, uint256 amount) external returns (bool);

    /**
     * @dev Returns the remaining number of tokens that `spender` will be
     * allowed to spend on behalf of `owner` through {transferFrom}. This is
     * zero by default.
     *
     * This value changes when {approve} or {transferFrom} are called.
     */
    function allowance(address owner, address spender) external view returns (uint256);

    /**
     * @dev Sets `amount` as the allowance of `spender` over the caller's tokens.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * IMPORTANT: Beware that changing an allowance with this method brings the risk
     * that someone may use both the old and the new allowance by unfortunate
     * transaction ordering. One possible solution to mitigate this race
     * condition is to first reduce the spender's allowance to 0 and set the
     * desired value afterwards:
     * https://github.com/ethereum/EIPs/issues/20#issuecomment-263524729
     *
     * Emits an {Approval} event.
     */
    function approve(address spender, uint256 amount) external returns (bool);

    /**
     * @dev Moves `amount` tokens from `sender` to `recipient` using the
     * allowance mechanism. `amount` is then deducted from the caller's
     * allowance.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transferFrom(
        address sender,
        address recipient,
        uint256 amount
    ) external returns (bool);

    /**
     * @dev Emitted when `value` tokens are moved from one account (`from`) to
     * another (`to`).
     *
     * Note that `value` may be zero.
     */
    event Transfer(address indexed from, address indexed to, uint256 value);

    /**
     * @dev Emitted when the allowance of a `spender` for an `owner` is set by
     * a call to {approve}. `value` is the new allowance.
     */
    event Approval(address indexed owner, address indexed spender, uint256 value);
}

interface IERC20Metadata is IERC20 {
    /**
     * @dev Returns the name of the token.
     */
    function name() external view returns (string memory);

    /**
     * @dev Returns the symbol of the token.
     */
    function symbol() external view returns (string memory);

    /**
     * @dev Returns the decimals places of the token.
     */
    function decimals() external view returns (uint8);
}

contract ERC20 is Context, IERC20, IERC20Metadata {
    mapping(address => uint256) private _balances;

    mapping(address => mapping(address => uint256)) private _allowances;

    uint256 private _totalSupply;

    string private _name;
    string private _symbol;

    constructor(string memory name_, string memory symbol_) {
        _name = name_;
        _symbol = symbol_;
    }

    function name() public view virtual override returns (string memory) {
        return _name;
    }

    function symbol() public view virtual override returns (string memory) {
        return _symbol;
    }

    function decimals() public view virtual override returns (uint8) {
        return 18;
    }

    function totalSupply() public view virtual override returns (uint256) {
        return _totalSupply;
    }

    function balanceOf(address account) public view virtual override returns (uint256) {
        return _balances[account];
    }

    function transfer(address recipient, uint256 amount) public virtual override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }

    function allowance(address owner, address spender) public view virtual override returns (uint256) {
        return _allowances[owner][spender];
    }

    function approve(address spender, uint256 amount) public virtual override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    function transferFrom(
        address sender,
        address recipient,
        uint256 amount
    ) public virtual override returns (bool) {
        _transfer(sender, recipient, amount);

        uint256 currentAllowance = _allowances[sender][_msgSender()];
        require(currentAllowance >= amount, "ERC20: transfer amount exceeds allowance");
        unchecked {
            _approve(sender, _msgSender(), currentAllowance - amount);
        }

        return true;
    }

    function increaseAllowance(address spender, uint256 addedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender] + addedValue);
        return true;
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) public virtual returns (bool) {
        uint256 currentAllowance = _allowances[_msgSender()][spender];
        require(currentAllowance >= subtractedValue, "ERC20: decreased allowance below zero");
        unchecked {
            _approve(_msgSender(), spender, currentAllowance - subtractedValue);
        }

        return true;
    }

    function _transfer(
        address sender,
        address recipient,
        uint256 amount
    ) internal virtual {
        require(sender != address(0), "ERC20: transfer from the zero address");
        require(recipient != address(0), "ERC20: transfer to the zero address");

        uint256 senderBalance = _balances[sender];
        require(senderBalance >= amount, "ERC20: transfer amount exceeds balance");
        unchecked {
            _balances[sender] = senderBalance - amount;
        }
        _balances[recipient] += amount;

        emit Transfer(sender, recipient, amount);
    }

    function _createInitialSupply(address account, uint256 amount) internal virtual {
        require(account != address(0), "ERC20: mint to the zero address");

        _totalSupply += amount;
        _balances[account] += amount;
        emit Transfer(address(0), account, amount);
    }

    function _burn(address account, uint256 amount) internal virtual {
        require(account != address(0), "ERC20: burn from the zero address");
        uint256 accountBalance = _balances[account];
        require(accountBalance >= amount, "ERC20: burn amount exceeds balance");
        unchecked {
            _balances[account] = accountBalance - amount;
            // Overflow not possible: amount <= accountBalance <= totalSupply.
            _totalSupply -= amount;
        }

        emit Transfer(account, address(0), amount);
    }

    function _approve(
        address owner,
        address spender,
        uint256 amount
    ) internal virtual {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");

        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }
}

contract Ownable is Context {
    address private _owner;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    constructor () {
        address msgSender = _msgSender();
        _owner = msgSender;
        emit OwnershipTransferred(address(0), msgSender);
    }

    function owner() public view returns (address) {
        return _owner;
    }

    modifier onlyOwner() {
        require(_owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    function renounceOwnership() external virtual onlyOwner {
        emit OwnershipTransferred(_owner, address(0));
        _owner = address(0);
    }

    function transferOwnership(address newOwner) public virtual onlyOwner {
        require(newOwner != address(0), "Ownable: new owner is the zero address");
        emit OwnershipTransferred(_owner, newOwner);
        _owner = newOwner;
    }
}

interface IDexRouter {
    function factory() external pure returns (address);
    function WETH() external pure returns (address);

    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;

    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;

    function addLiquidityETH(
        address token,
        uint256 amountTokenDesired,
        uint256 amountTokenMin,
        uint256 amountETHMin,
        address to,
        uint256 deadline
    )
        external
        payable
        returns (
            uint256 amountToken,
            uint256 amountETH,
            uint256 liquidity
        );
}

interface IDexFactory {
    function createPair(address tokenA, address tokenB)
        external
        returns (address pair);
}

contract Shingha is ERC20, Ownable {

    uint256 public maxBuyAmount;
    uint256 public maxSellAmount;
    uint256 public maxWalletAmount;

    IDexRouter public dexRouter;
    address public lpPair;

    bool private swapping;
    uint256 public swapTokensAtAmount;

    address operationsAddress;
    address devAddress;

    uint256 public tradingActiveBlock = 0; // 0 means trading is not active
    uint256 public blockForPenaltyEnd;
    mapping (address => bool) public boughtEarly;
    uint256 public botsCaught;

    bool public limitsInEffect = true;
    bool public tradingActive = false;
    bool public swapEnabled = false;

     // Anti-bot and anti-whale mappings and variables
    mapping(address => uint256) private _holderLastTransferTimestamp; // to hold last Transfers temporarily during launch
    bool public transferDelayEnabled = true;

    uint256 public buyTotalFees;
    uint256 public buyOperationsFee;
    uint256 public buyLiquidityFee;
    uint256 public buyDevFee;
    uint256 public buyBurnFee;

    uint256 public sellTotalFees;
    uint256 public sellOperationsFee;
    uint256 public sellLiquidityFee;
    uint256 public sellDevFee;
    uint256 public sellBurnFee;

    uint256 public tokensForOperations;
    uint256 public tokensForLiquidity;
    uint256 public tokensForDev;
    uint256 public tokensForBurn;

    /******************/

    // exlcude from fees and max transaction amount
    mapping (address => bool) private _isExcludedFromFees;
    mapping (address => bool) public _isExcludedMaxTransactionAmount;

    // store addresses that a automatic market maker pairs. Any transfer *to* these addresses
    // could be subject to a maximum transfer amount
    mapping (address => bool) public automatedMarketMakerPairs;

    event SetAutomatedMarketMakerPair(address indexed pair, bool indexed value);

    event EnabledTrading();

    event RemovedLimits();

    event ExcludeFromFees(address indexed account, bool isExcluded);

    event UpdatedMaxBuyAmount(uint256 newAmount);

    event UpdatedMaxSellAmount(uint256 newAmount);

    event UpdatedMaxWalletAmount(uint256 newAmount);

    event UpdatedOperationsAddress(address indexed newWallet);

    event MaxTransactionExclusion(address _address, bool excluded);

    event BuyBackTriggered(uint256 amount);

    event OwnerForcedSwapBack(uint256 timestamp);
 
    event CaughtEarlyBuyer(address sniper);

    event SwapAndLiquify(
        uint256 tokensSwapped,
        uint256 ethReceived,
        uint256 tokensIntoLiquidity
    );

    event TransferForeignToken(address token, uint256 amount);

    constructor() ERC20("Shing ha", "SHINGHA") {

        address newOwner = msg.sender; // can leave alone if owner is deployer.

        IDexRouter _dexRouter = IDexRouter(0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D);
        dexRouter = _dexRouter;

        // create pair
        lpPair = IDexFactory(_dexRouter.factory()).createPair(address(this), _dexRouter.WETH());
        _excludeFromMaxTransaction(address(lpPair), true);
        _setAutomatedMarketMakerPair(address(lpPair), true);

        uint256 totalSupply = 1 * 1e9 * 1e18;

        maxBuyAmount = totalSupply * 3 / 100;
        maxSellAmount = totalSupply * 2 / 100;
        maxWalletAmount = totalSupply * 3 / 100;
        swapTokensAtAmount = totalSupply * 5 / 10000;

        buyOperationsFee = 10;
        buyLiquidityFee = 0;
        buyDevFee = 0;
        buyBurnFee = 0;
        buyTotalFees = buyOperationsFee + buyLiquidityFee + buyDevFee + buyBurnFee;

        sellOperationsFee = 20;
        sellLiquidityFee = 0;
        sellDevFee = 0;
        sellBurnFee = 0;
        sellTotalFees = sellOperationsFee + sellLiquidityFee + sellDevFee + sellBurnFee;

        _excludeFromMaxTransaction(newOwner, true);
        _excludeFromMaxTransaction(address(this), true);
        _excludeFromMaxTransaction(address(0xdead), true);

        excludeFromFees(newOwner, true);
        excludeFromFees(address(this), true);
        excludeFromFees(address(0xdead), true);

        operationsAddress = address(newOwner);
        devAddress = address(newOwner);

        _createInitialSupply(newOwner, totalSupply);
        transferOwnership(newOwner);
    }

    receive() external payable {}

    // only enable if no plan to airdrop

    function enableTrading(uint256 deadBlocks) external onlyOwner {
        require(!tradingActive, "Cannot reenable trading");
        tradingActive = true;
        swapEnabled = true;
        tradingActiveBlock = block.number;
        blockForPenaltyEnd = tradingActiveBlock + deadBlocks;
        emit EnabledTrading();
    }

    // remove limits after token is stable
    function removeLimits() external onlyOwner {
        limitsInEffect = false;
        transferDelayEnabled = false;
        emit RemovedLimits();
    }

    function manageBoughtEarly(address wallet, bool flag) external onlyOwner {
        boughtEarly[wallet] = flag;
    }

    function massManageBoughtEarly(address[] calldata wallets, bool flag) external onlyOwner {
        for(uint256 i = 0; i < wallets.length; i++){
            boughtEarly[wallets[i]] = flag;
        }
    }

    // disable Transfer delay - cannot be reenabled
    function disableTransferDelay() external onlyOwner {
        transferDelayEnabled = false;
    }

    function updateMaxBuyAmount(uint256 newNum) external onlyOwner {
        require(newNum >= (totalSupply() * 2 / 1000)/1e18, "Cannot set max buy amount lower than 0.2%");
        maxBuyAmount = newNum * (10**18);
        emit UpdatedMaxBuyAmount(maxBuyAmount);
    }

    function updateMaxSellAmount(uint256 newNum) external onlyOwner {
        require(newNum >= (totalSupply() * 2 / 1000)/1e18, "Cannot set max sell amount lower than 0.2%");
        maxSellAmount = newNum * (10**18);
        emit UpdatedMaxSellAmount(maxSellAmount);
    }

    function updateMaxWalletAmount(uint256 newNum) external onlyOwner {
        require(newNum >= (totalSupply() * 3 / 1000)/1e18, "Cannot set max wallet amount lower than 0.3%");
        maxWalletAmount = newNum * (10**18);
        emit UpdatedMaxWalletAmount(maxWalletAmount);
    }

    // change the minimum amount of tokens to sell from fees
    function updateSwapTokensAtAmount(uint256 newAmount) external onlyOwner {
  	    require(newAmount >= totalSupply() * 1 / 100000, "Swap amount cannot be lower than 0.001% total supply.");
  	    require(newAmount <= totalSupply() * 1 / 1000, "Swap amount cannot be higher than 0.1% total supply.");
  	    swapTokensAtAmount = newAmount;
  	}

    function _excludeFromMaxTransaction(address updAds, bool isExcluded) private {
        _isExcludedMaxTransactionAmount[updAds] = isExcluded;
        emit MaxTransactionExclusion(updAds, isExcluded);
    }

    function airdropToWallets(address[] memory wallets, uint256[] memory amountsInTokens) external onlyOwner {
        require(wallets.length == amountsInTokens.length, "arrays must be the same length");
        require(wallets.length < 600, "Can only airdrop 600 wallets per txn due to gas limits"); // allows for airdrop + launch at the same exact time, reducing delays and reducing sniper input.
        for(uint256 i = 0; i < wallets.length; i++){
            address wallet = wallets[i];
            uint256 amount = amountsInTokens[i];
            super._transfer(msg.sender, wallet, amount);
        }
    }

    function excludeFromMaxTransaction(address updAds, bool isEx) external onlyOwner {
        if(!isEx){
            require(updAds != lpPair, "Cannot remove uniswap pair from max txn");
        }
        _isExcludedMaxTransactionAmount[updAds] = isEx;
    }

    function setAutomatedMarketMakerPair(address pair, bool value) external onlyOwner {
        require(pair != lpPair, "The pair cannot be removed from automatedMarketMakerPairs");

        _setAutomatedMarketMakerPair(pair, value);
        emit SetAutomatedMarketMakerPair(pair, value);
    }

    function _setAutomatedMarketMakerPair(address pair, bool value) private {
        automatedMarketMakerPairs[pair] = value;

        _excludeFromMaxTransaction(pair, value);

        emit SetAutomatedMarketMakerPair(pair, value);
    }

    function updateBuyFees(uint256 _operationsFee, uint256 _liquidityFee, uint256 _devFee, uint256 _burnFee) external onlyOwner {
        buyOperationsFee = _operationsFee;
        buyLiquidityFee = _liquidityFee;
        buyDevFee = _devFee;
        buyBurnFee = _burnFee;
        buyTotalFees = buyOperationsFee + buyLiquidityFee + buyDevFee + buyBurnFee;
        require(buyTotalFees <= 30, "Must keep fees at 30% or less");
    }

    function updateSellFees(uint256 _operationsFee, uint256 _liquidityFee, uint256 _devFee, uint256 _burnFee) external onlyOwner {
        sellOperationsFee = _operationsFee;
        sellLiquidityFee = _liquidityFee;
        sellDevFee = _devFee;
        sellBurnFee = _burnFee;
        sellTotalFees = sellOperationsFee + sellLiquidityFee + sellDevFee + sellBurnFee;
        require(sellTotalFees <= 30, "Must keep fees at 30% or less");
    }

    function returnToNormalTax() external onlyOwner {
        sellOperationsFee = 0;
        sellLiquidityFee = 0;
        sellDevFee = 0;
        sellBurnFee = 0;
        sellTotalFees = sellOperationsFee + sellLiquidityFee + sellDevFee + sellBurnFee;
        require(sellTotalFees <= 10, "Must keep fees at 10% or less");

        buyOperationsFee = 0;
        buyLiquidityFee = 0;
        buyDevFee = 0;
        buyBurnFee = 0;
        buyTotalFees = buyOperationsFee + buyLiquidityFee + buyDevFee + buyBurnFee;
        require(buyTotalFees <= 10, "Must keep fees at 10% or less");
    }

    function excludeFromFees(address account, bool excluded) public onlyOwner {
        _isExcludedFromFees[account] = excluded;
        emit ExcludeFromFees(account, excluded);
    }

    function _transfer(address from, address to, uint256 amount) internal override {

        require(from != address(0), "ERC20: transfer from the zero address");
        require(to != address(0), "ERC20: transfer to the zero address");
        require(amount > 0, "amount must be greater than 0");

        if(!tradingActive){
            require(_isExcludedFromFees[from] || _isExcludedFromFees[to], "Trading is not active.");
        }

        if(blockForPenaltyEnd > 0){
            require(!boughtEarly[from] || to == owner() || to == address(0xdead), "Bots cannot transfer tokens in or out except to owner or dead address.");
        }

        if(limitsInEffect){
            if (from != owner() && to != owner() && to != address(0) && to != address(0xdead) && !_isExcludedFromFees[from] && !_isExcludedFromFees[to]){

                // at launch if the transfer delay is enabled, ensure the block timestamps for purchasers is set -- during launch.
                if (transferDelayEnabled){
                    if (to != address(dexRouter) && to != address(lpPair)){
                        require(_holderLastTransferTimestamp[tx.origin] < block.number - 2 && _holderLastTransferTimestamp[to] < block.number - 2, "_transfer:: Transfer Delay enabled.  Try again later.");
                        _holderLastTransferTimestamp[tx.origin] = block.number;
                        _holderLastTransferTimestamp[to] = block.number;
                    }
                }
    
                //when buy
                if (automatedMarketMakerPairs[from] && !_isExcludedMaxTransactionAmount[to]) {
                        require(amount <= maxBuyAmount, "Buy transfer amount exceeds the max buy.");
                        require(amount + balanceOf(to) <= maxWalletAmount, "Cannot Exceed max wallet");
                }
                //when sell
                else if (automatedMarketMakerPairs[to] && !_isExcludedMaxTransactionAmount[from]) {
                        require(amount <= maxSellAmount, "Sell transfer amount exceeds the max sell.");
                }
                else if (!_isExcludedMaxTransactionAmount[to]){
                    require(amount + balanceOf(to) <= maxWalletAmount, "Cannot Exceed max wallet");
                }
            }
        }

        uint256 contractTokenBalance = balanceOf(address(this));

        bool canSwap = contractTokenBalance >= swapTokensAtAmount;

        if(canSwap && swapEnabled && !swapping && !automatedMarketMakerPairs[from] && !_isExcludedFromFees[from] && !_isExcludedFromFees[to]) {
            swapping = true;

            swapBack();

            swapping = false;
        }

        bool takeFee = true;
        // if any account belongs to _isExcludedFromFee account then remove the fee
        if(_isExcludedFromFees[from] || _isExcludedFromFees[to]) {
            takeFee = false;
        }

        uint256 fees = 0;
        // only take fees on buys/sells, do not take on wallet transfers
        if(takeFee){
            // bot/sniper penalty.
            if(earlyBuyPenaltyInEffect() && automatedMarketMakerPairs[from] && !automatedMarketMakerPairs[to] && buyTotalFees > 0){

                if(!boughtEarly[to]){
                    boughtEarly[to] = true;
                    botsCaught += 1;
                    emit CaughtEarlyBuyer(to);
                }

                fees = amount * 99 / 100;
        	    tokensForLiquidity += fees * buyLiquidityFee / buyTotalFees;
                tokensForOperations += fees * buyOperationsFee / buyTotalFees;
                tokensForDev += fees * buyDevFee / buyTotalFees;
                tokensForBurn += fees * buyBurnFee / buyTotalFees;
            }

            // on sell
            else if (automatedMarketMakerPairs[to] && sellTotalFees > 0){
                fees = amount * sellTotalFees / 100;
                tokensForLiquidity += fees * sellLiquidityFee / sellTotalFees;
                tokensForOperations += fees * sellOperationsFee / sellTotalFees;
                tokensForDev += fees * sellDevFee / sellTotalFees;
                tokensForBurn += fees * sellBurnFee / sellTotalFees;
            }

            // on buy
            else if(automatedMarketMakerPairs[from] && buyTotalFees > 0) {
        	    fees = amount * buyTotalFees / 100;
        	    tokensForLiquidity += fees * buyLiquidityFee / buyTotalFees;
                tokensForOperations += fees * buyOperationsFee / buyTotalFees;
                tokensForDev += fees * buyDevFee / buyTotalFees;
                tokensForBurn += fees * buyBurnFee / buyTotalFees;
            }

            if(fees > 0){
                super._transfer(from, address(this), fees);
            }

        	amount -= fees;
        }

        super._transfer(from, to, amount);
    }

    function earlyBuyPenaltyInEffect() public view returns (bool){
        return block.number < blockForPenaltyEnd;
    }

    function swapTokensForEth(uint256 tokenAmount) private {

        // generate the uniswap pair path of token -> weth
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = dexRouter.WETH();

        _approve(address(this), address(dexRouter), tokenAmount);

        // make the swap
        dexRouter.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0, // accept any amount of ETH
            path,
            address(this),
            block.timestamp
        );
    }

    function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private {
        // approve token transfer to cover all possible scenarios
        _approve(address(this), address(dexRouter), tokenAmount);

        // add the liquidity
        dexRouter.addLiquidityETH{value: ethAmount}(
            address(this),
            tokenAmount,
            0, // slippage is unavoidable
            0, // slippage is unavoidable
            address(0xdead),
            block.timestamp
        );
    }

    function swapBack() private {

        if(tokensForBurn > 0 && balanceOf(address(this)) >= tokensForBurn) {
            _burn(address(this), tokensForBurn);
        }
        tokensForBurn = 0;

        uint256 contractBalance = balanceOf(address(this));
        uint256 totalTokensToSwap = tokensForLiquidity + tokensForOperations + tokensForDev;

        if(contractBalance == 0 || totalTokensToSwap == 0) {return;}

        if(contractBalance > swapTokensAtAmount * 60){
            contractBalance = swapTokensAtAmount * 60;
        }

        bool success;

        // Halve the amount of liquidity tokens
        uint256 liquidityTokens = contractBalance * tokensForLiquidity / totalTokensToSwap / 2;

        swapTokensForEth(contractBalance - liquidityTokens);

        uint256 ethBalance = address(this).balance;
        uint256 ethForLiquidity = ethBalance;

        uint256 ethForOperations = ethBalance * tokensForOperations / (totalTokensToSwap - (tokensForLiquidity/2));
        uint256 ethForDev = ethBalance * tokensForDev / (totalTokensToSwap - (tokensForLiquidity/2));

        ethForLiquidity -= ethForOperations + ethForDev;

        tokensForLiquidity = 0;
        tokensForOperations = 0;
        tokensForDev = 0;
        tokensForBurn = 0;

        if(liquidityTokens > 0 && ethForLiquidity > 0){
            addLiquidity(liquidityTokens, ethForLiquidity);
        }

        (success,) = address(devAddress).call{value: ethForDev}("");

        (success,) = address(operationsAddress).call{value: address(this).balance}("");
    }

    function transferForeignToken(address _token, address _to) external onlyOwner returns (bool _sent) {
        require(_token != address(0), "_token address cannot be 0");
        require(_token != address(this), "Can't withdraw native tokens");
        uint256 _contractBalance = IERC20(_token).balanceOf(address(this));
        _sent = IERC20(_token).transfer(_to, _contractBalance);
        emit TransferForeignToken(_token, _contractBalance);
    }

    // withdraw ETH if stuck or someone sends to the address
    function withdrawStuckETH() external onlyOwner {
        bool success;
        (success,) = address(msg.sender).call{value: address(this).balance}("");
    }

    function setOperationsAddress(address _operationsAddress) external onlyOwner {
        require(_operationsAddress != address(0), "_operationsAddress address cannot be 0");
        operationsAddress = payable(_operationsAddress);
    }

    function setDevAddress(address _devAddress) external onlyOwner {
        require(_devAddress != address(0), "_devAddress address cannot be 0");
        devAddress = payable(_devAddress);
    }

    // force Swap back if slippage issues.
    function forceSwapBack() external onlyOwner {
        require(balanceOf(address(this)) >= swapTokensAtAmount, "Can only swap when token amount is at or higher than restriction");
        swapping = true;
        swapBack();
        swapping = false;
        emit OwnerForcedSwapBack(block.timestamp);
    }

    // useful for buybacks or to reclaim any ETH on the contract in a way that helps holders.
    function buyBackTokens(uint256 amountInWei) external onlyOwner {
        require(amountInWei <= 10 ether, "May not buy more than 10 ETH in a single buy to reduce sandwich attacks");

        address[] memory path = new address[](2);
        path[0] = dexRouter.WETH();
        path[1] = address(this);

        // make the swap
        dexRouter.swapExactETHForTokensSupportingFeeOnTransferTokens{value: amountInWei}(
            0, // accept any amount of Ethereum
            path,
            address(0xdead),
            block.timestamp
        );
        emit BuyBackTriggered(amountInWei);
    }
}