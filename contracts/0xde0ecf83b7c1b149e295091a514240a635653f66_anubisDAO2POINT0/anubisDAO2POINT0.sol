/**
 *Submitted for verification at Etherscan.io on 2023-06-29
*/

/*
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]@[email protected]@%8BBB%B&&Md0c|]~l^. ',<[|nLb*W88B%%B%%%@@B%@@[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]%%88#Qt+^.''...... ..............''.'..`~tY#%@B%[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]%@%[email protected];..'' .'-upoW8%[email protected]@%@@@[email protected]@[email protected]#adu-``` ' `"?J*@&@[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%@B&h[.`.'^^>L%8B%@@B88B8%8MMM#MMooo##MW&%BB%%@B%%@BBBZ+". ' [email protected]@@[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BB%L]"` .;/m8B%@BBBMdCj]ili!ii!!!!>+11-i!!!!i!i!!>[[email protected]@%%%8dtI.'.'-C8%%[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%@Wn"^'."[email protected]&Boj_i+i!i!!!!!!iil"xo#/>-(hWuIii>ii!!!!i>l;+</[email protected]@%8%WX:.` :(#[email protected]%@[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BBh|^..`)[email protected]@Bkj<;+Iii!!il>!l!!!!i!iYWhI>iiiIm8Ziilii!!!i!I!~>;!!~l<udB%[email protected]#),'`.1hB%[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@%%%BBBj"' ,uMB%B&dn-l![QWa*%av~!i<;>!!!!!i~$%cii>i!>_B%?lil!!!ii!l<!t#%MoWO{!>[email protected]%%8Mu"'.`|#B%B%[email protected]@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@BB%Bpl^` tW&8BBO}!l!i<a0:>!i!L%B1!l!!!!!!!l>kB0!!I>liu8*li!ii!!!>[email protected]_Iii+ZW(;[email protected]%Mr`,[email protected]@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@B%%v`^ !k%8&B0-;!i!il+#8[!i!>iIq%hli<li!!!!!l.w%ni~ii/BwIi!!!!!i!l!!bMqili>!i-#&?Ill>l<~Z8&BBa?''`rW%@[email protected]@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@B8Wt'''|&B&Bd1I!l>!ii!ilM%cll>l!>!Bpii!!l!!!l>|oUUZoI!wmcUaXl>[email protected]{i!i!<l(@B]!ii!!iiIi[h%%8&v`'`|#%[email protected]@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@[email protected]).'`z%*[email protected]<!:!i!>!i!i!ic%%f,i!>:Ch!18+>!!i!l;-vzXXr:itXCUL|!!>i!iiihflaZ!<<<l)*8J<i!!!!!!i~:-!xBB%BY,'`]BB%[email protected]@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@%%j`^"[email protected]!<i!lIiii>i_jrX[rqB8C(l>8&88kt<I>~[/XqM%[email protected]@BBBB%W*mc|]<<>I)p&[email protected]%+~<XM&k/?YQn1;><iil<+l?"j8&B8C,' j&@[email protected]@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@B%BO,^'xB%BWjiii!i!l<!!ljW&%B8BBBWm*B/l??>(Uh&[email protected]@@@@@@[email protected]@@BB%BBBBoCt~_[>1B8Z*8%BB8%8%z!>ilil!Iii>|&%BWU.`,UBB%@@@@@@@@@@@@@@@
@@@@@@@@@@@@%B%@%&<` -&[email protected]#j;+L%[email protected]%@#[l>|B%&Y/jrO&%8%&U0&@@%%BBBBabqO00OO00ZZO0O0QOZqqo%[email protected]%%@&Qn*@%8&kYunYW88Ll>>[email protected]!!(MB8B}`.;[email protected]@%[email protected]@@@@@@@@@@
@@@@@@@@@@@@BB%8J``;m%BBClIvp<+I!!Y%8z<oBBY-_}/ft|CWB8%BB&aqOQQOOOb&B%[email protected]@%[email protected]@@%[email protected]@&[email protected]%pvnvuj)tn8B%[email protected]>>iIlwLIizBW8b: "XB%@@@@@@@@@@@@@
@@@@@@@@@@@@[email protected]#+"'/8%%d?I>j%1i!ii!!i*8]BB%!`.'"-|(f/u*%%BkZQ0OOd&@BB&[email protected]%[email protected]%&Ouunu/><<<<#[email protected]]<i!lii~%Xii>b8B%t"^+*%[email protected]@@@@@@@@@@
@@@@@@@@@@@BBw```d8BBnl>l!wBc!il<!>lfbf88B'.'^'':|t/f/c&[email protected]&@@%&%@%%[email protected]@@@[email protected]%%&&@@@[email protected][<;><>[email protected]*c!>i!!li[@ol!i;r8%@#"'"[email protected]@[email protected]@@@@@@@
@@@@@@@@@[email protected]'`i8B8*<>iii!>MBp_l<l>!x&@[email protected]`!MW1ltt/t(/0%%@a8%[email protected]/|((|t/cOb#[email protected]%Bo%@%*xcxvcx}-o8u<>[email protected]@%vllI<I<OBB|>;~Ii<b%@W~^[email protected]%@@@@@@@
@@@@@@@@@B8r`'1%@%J>i!i!!>~:LBWBkd*]:>|%@@  '[email protected]%@Yl//tt)/ja%%B%88%aU/tt/jnnnxr|/|/zh%BB%%BBMUnvnvnz]n&[email protected]<><wBBq~i?dkpM&80<!i!i!!>lU%8Br `18%%@@@@@@@
@@@@@@@@@&(``v%%&f>!!>!!!ii>"ll>[email protected]~|#[email protected]` [email protected]@%[email protected]#n]r/(/t)U&@%pv&%%[email protected]@[email protected]@@@B&BB&%00%BBqvvuxvvf/#%[email protected]@~>>[email protected]~l>!>;!>iii!l<<j&%@z^`[email protected]@@@@@@
@@@@@@@BB{^'XB881!!il;l!i!I>lildBo|i*%@@@@;..#%%BB8B%bxx/|/)jp#m|/b%@#Jjt/||/tnUMBBacn0aWJrvuxvvZ%&@@BBBB<>>M%BBB&-1#@L~iiil>!iil!l!i[&%@C^`<[email protected]@@@@@
@@@@@@BB)''[email protected]}l!ii!ii!!!!i!i!>[email protected]#[email protected]@+  L8B#qwd&@[email protected]%ou/ttt/tf/q&%Cf|//j//tz%BanuuuvnunXh%[email protected]%8akbMBB8>[email protected]@#@BBQlli!!!!!!!!!i!!!!!?WB%J''~8%@%@@@
@@@@@@Br.'x%%W1~!!i!ii!!!!!!!iiI>[email protected]%[email protected]@).'Iv,^ ''.LB#X|f||/tf/t/n%88BBBB%B%B%8B0vnnzunnxzcUqW#?<~<<Ir/>>-BB%[email protected]&W>l>l!!!!ii!!iii!i>[email protected]@Y `|%B%[email protected]@
@@@@@B0^ |[email protected]|!i1w8%bn+liIiliiI/[email protected]#Z00bBBO... ...^`(Jx|/tttttt////j&B%b0XvnvCq8B8JuuuuuuuuuuxvuLC~;<>i<!i>[email protected]*[email protected]%@xi>!I!>>I:f0%#J)I>-8%Bj^ YB%[email protected]@
@@@@Ba. <[email protected]%f!)WmvnU*%8L!oz]l!XBB%[email protected]@8".'.ij]~|///|///tt//////fM%Bc/|t//tv#B8uxnuuuuuuuuuuvxvx)1z(l<!<[email protected]%%BL;<[JZin8%&Zvcw#f<t8B&+ '[email protected]@@
@@@[email protected]^ *%@O>)d-~ili>ln%[email protected]!UB%[email protected]@Bj';#%88v(t|ttt/t////tttt|wB%%BB%@&BWB%oxvuuuuuuuuuunuuvnv*[email protected]:)[email protected]%bOOQZQp&@@p!qBUfBJli<I!+Id{!z%%8,.,@@[email protected]
@@%@f''[email protected]%aliqJ+!iliiiiYb%#~L8%8w0OmQO0M%@@[email protected]%8qj|/t//////////tttt|U%BW*&%B8oo%BdnvuuuuuuuuuuuuunuuxO88%[email protected]%m00O0Qm%B%dIh%k*~il>li!<f#i>w%BZ''[[email protected]@
@BBW`'[email protected]}[email protected]!!i)>U%[email protected]%@@B%8ar/ttttt/////////tt//|x%@M&8B%@%hBBOvunuuuuuuuuuuuuuvuuuxh%B%@BBB&OOOOO0Z8%&m>|>li>ii!i;u8pii]@BB[.`[email protected]@
@BW+ '[email protected]~Ilq8%&OttZW%p<{BBBmOOOOO0q%BB&BBBO|//tttt///ttt/ttt/f//ffB%B%q1(L8%@%CxcuuuuuuuuuuuuuuuuuuvxCWBBa%[email protected]%@Xiq8&o}-0M%Bal!>[email protected]* ',B%
@BW''}BB8-iI!;~;XqhkL<o&J<%[email protected]%@B%Q)t//ttttttttt////t//ftC&B8qro#oMzwW%8pxvuuuuuuuuuuuuuuuuuuvxC&%B%BB%kQOZOOOpBB%{[email protected]<Jb*hc-Ii<li_BBBx'^[email protected]
@8+.'[email protected]<!ii!i>;[email protected]@[email protected]%*[email protected]/t/ttt/tt/j|/fxUbW&bc|vW8BZtLoYJkk)L8B%CxzpW8#[email protected]%8Bw0OZ0OO*@BhiZW&vlil>il!>[email protected]@h.'~B
B#" }[email protected]}!lIii!i!!>ll!~+/%8%w0OOO00hB%&jWBBO/t/ttttttt(xh&[email protected]@BBB%WY(m88&wa8WaJ&88bvn*B%@[email protected]%8|dB%&[email protected]%X~+I+!;iiiii!>l>!&[email protected]?' h
&Q''UB%p!<!I>ii!i!iil>>l*8%*QOZLOQb%B%L|MB8X/t/ttttttff//[email protected]%BBBWcj)bB8&xn#%B*cvrW%%%%@%BB#rvuuuuuuuvvvnz%%&xtMBB#OQ0OZ0k%B*!ll!i!li!!!i!il;[email protected] 'n
B~ .#@B/iilii!iiii!iii~([email protected]@B8B&BB8u///////tttftttZMB8%BBB%C||/xd%BBB*Jxuuv%%BB%%B8aYvvunuuuuuuunnu%BB#[email protected]@@[email protected]' ~
W.'[email protected]_i!iii>ii!iii!>;[email protected]%%t////tt////(/|/|(/r(/nZ*dj(1)(XoaCnvcvvO*[email protected]%@B*bBBBO0O0O0#%B*!iii!!!!!!!!!il<WBBl.'
b. [email protected]!?1(_i!jYU}<[email protected]%&nvuQ%B&/tt|t///?l^`''`''`"!+)|t/t/t///t/ruuuuvvnvn(-~i!i>><i>~)nxvnuuuuW%BbcnxpBB&[email protected]@&+?UYXiI~{)+i<IilhB%f'.
Q' [email protected]!-dB%BB%%kiCB/<@B%mQO00q8B%Yj1tn&@8ff/ttt?,'..   . .. '`'l{t/f/tttt/ruuucnnn]<i>>i>ii>>!!<!>1uuuuuu&BB0f|ft*%@o0OOOZ%[email protected]%piwBBB$&Ww>>[email protected]` 
X  [email protected]@ml-#Z];!>[email protected]}tBB%[email protected]%@8X(/n8B%j|tj/_`. .           ..`_///ttt//rvnvxvj<>>>>>>>>>>i>>>!+!]urvuvM%8Uj|rp%B%BkQZ0O&%@|)@b%XiI!!l<[email protected]&. 
v [email protected]@Q!u0llIii!l>il][email protected]@8O0ZQ#BB&%B%8hUMB%vt|t1^.              . .^_t//////xuuuvf~>>>>>>>>>>>>>i><>>i|nunu%B8Up88BB8%BBmOOQW%%v<_l<[email protected]'.
n''[email protected]!I!ii!<>[[email protected]&JuCM%%@%8%Q(tt~`..             .. .^I)///t/runn}i<i>>>>>>>>>>ii>>>>ii~xvuY8BBBBBWpXuaB%[email protected])[email protected]@B' 
c''[email protected]%L+~k%UI;!^)&[email protected]&00m8B%C)ffcvL&%[email protected]/|)I. . ... . ..`.    '^"`'!|rf/vuxi<~<!>>>>><!!I!>>>>>>>><[email protected]@%dzunt(jW%%[email protected]@z}Ba081il,>r%h>[email protected]''
U  [email protected]@bil!rwBB%%*r!dW>[email protected]@8c|/|t/tcUBBWjt/,........!1<'.'.   .'(&[email protected]%BB%B%B%q-i>>>ii<!_j}i~i>>>>>>xxu*&Bmnutt/||tbBBBqO&@B(]Bh;j*B8&[email protected]@*..
Z..LBBM!!iilii>!!!1fnl<&@[email protected]@B8pu(t|t/M%%p1(; .......b%Bd`'     [email protected]@@@@@@@[email protected]>>~!>i!X%8%}<ii>>>>>rnJBBBc|(t(tQ*B%[email protected]@8wBBBi!(([l!l!i!l!il!qB%q. 
h  )@@W!!!i!i!l!>i<!><>[email protected]@k%%BJO88%&%qj/*%@%#(/l.     .^ wB%a^     `M%%[email protected]@@@@[email protected]@]>!>i<!0B%M_<!~ii>>><jxb%B%&c{LW%%B%kY#@@WB&#~~!<><ilii!!i!iloB%( .
&`.;%@%~i>l!i!ii!il>>[email protected]@%%ptvvcq8%[email protected]%Zt~`.. .  '.^[email protected]    'IW%[email protected]@@@@BBB%n<<!>~lu%8#+<!~i>>>>>+vU8BB%BBBB%%*Lrvxv%8BBBh!i>ll!!!ii!!liI~8B%".'
B{[email protected]@xii!iii!!!!i!!>>]@&@B#ft(/[email protected]&bx88%z(`....     "BB%n'.. .'^[email protected]%%@%B%m-ii<>[email protected]@1l>i>>>i>>>jv&%%zU&[email protected]|t|[email protected]%[<i!i!!!!!!!>iiivBB* `1
Bq [email protected]&>[email protected]%[email protected]|tt(/[email protected]|tX%%&X>.'..     '"WB&<.' '. ^. :]1?!~?+ii!>i~#BBu><i>>>>>>![u*%BO/t08%%/(//|/z#B8%Ba^]:[email protected] 'Y
@&`[email protected]{i>>i!ii!i!-w%@jWB8W&%%%WCf|L%B#ff/(Y%B*J'..'     ..t&%8Ot[1nw&B%@BBBB%&kQntrq%8%k>!>i>>i~;~iu#88q////b%Bqf|chBB8%*%@%c&8O{I!<i!I>!<;+%@B~.`o
@B/[email protected][m#B%MX+&%%%oYvYd&%B%M8B&z/////r&&BQ`''    ' ''iZ&%%BBB%%%%%%%%%%%[email protected]@B#tI>!~i>>><!ic8%BX/(j/jf#[email protected]#&%[email protected]&)vo&8#C+l<!!>[email protected]')B
@@@,'[email protected]@@1l!ip&@q/iI]M%88%jtjnzuco%@@BQ(j||/I: [email protected]%t.         . .m%B*ZwwwwmmwwqZ0a&[email protected]<I>!ii>>>+i<i]M%B&l^;))j/tuBBBB&LcxXt([email protected]@*{ilthBWQ!!i-%%8).'bB
BB%/'`[email protected]!(8Xi+!ll>I#@%8Jj|//rznwB%atf|t}". ''-#%%q         ..':8%&mOOOOOZZZ0mm#BBx>i!ii>>>il<iC%WWf. ..`_t//|OB%oxUn|tt([email protected]@[email protected]}<Iw88q'`+BB
BB%&I :&@Br;(*iI?ll>IO%B%B8%*U(tfn8B8c/|t+^'..'''.I},         ...`n%%WOZZZZmZZZOo8%k!>i>>i>>>i>i<i/1!". ..."I//ffM%&C1j(xk%[email protected]@%W_<i<[email protected]{>j%&@+`^%[email protected]
@[email protected] 'rB&&>!p/;[email protected]%BYUoB%%BacpBBh//|I.'..                    ' w8M8oZ00ZQZdW%##i<>>>i>>>>>>i>><>!>l^  . ."{f1LB%#jZ8B%%&mr&B%OI>~i!}W>~&[email protected]` [email protected]@@
@@[email protected])'`ZBBMiIC%hh8BBW%[email protected]%%%Z<'....                     .`_d%B%[email protected]@%v>><>>>i>>>>>>i>>ii<ii<!I:^``;J8MB%%%B%Wqzucrj&B&WkqqWq:>[email protected]`[email protected]@
@@@@B*`.:o%@L!ii{uzd%B&rtf|rvcc0#%%[email protected]%B#)^`....              .'''....'i)tvcnx?>~!>>i!i><>>>>>>>>>i>>ii>~!~l)hBBB%@BB%bzvcrftf/w8BWLC)[email protected]%M: "#@[email protected]@
@@@@%BM.'!B%%[email protected]%omn)|j/ncucq8%[email protected]@@[email protected],.^ '             '[email protected]]'"..''`!~>~<_;][email protected]%U~<>>>>>>>>>i>>i!<>!Q%BBBB%B8kUucxtr)(jY#&8%8Llliln%@B~^[email protected]@@
@@@@BB%Q'`-%B%n>l!OB%B%88B%B#Ouf/rzxzb8BB%@B&W(^.  .          .`xW8%%d(<:I>i<i_tZ&@[email protected]!i>>>>>>>i><i><I/#%BBBB%%*Qzxuj(rJhW%B8BB%BB%_>I/@%%(`[email protected]%[email protected]@
@@@[email protected]@X^.}%[email protected][%%Br|fuO#%%8B%[email protected]%%B&%w>..'         ..`,)a%&MBBB%%BBB%B%#n~!>>>>>>>>>>>><[email protected]%BB*QcxzrfC*&%%8B8pUj/([email protected];[email protected](''c%[email protected]@@@@
@@@@[email protected]%@@X''][email protected]%JoB%bfj|){u||Jb&B%%8%U(zvuChBB%&[email protected]'          . ^`lI<~nd&W#Wau(<I<>+<>>>>>>>>i<>>tM&%%%%%WOzucjrhBBBB%Mwx|j|{|/fz%@%ZBBB)`[email protected]@@@@@@@
@@@@@@@@BBU''i8B&%BB88dUn//wO0w/jUh%BB%MXnvcu0*[email protected]%8Bh<"'   '      'I>i>>>i>>i>>>>i>>iiii<i!>!~_m&BBBBBWqXvzxxb%88%MZx(rkChu/fvmM88%[email protected]? 'xB&@@@@@@@
@@@@@@@@@BBw''lW%88#8BB8%%WqJv{|/t|jrb%8%BpxxczuZW%@@8%WJ` '       . "i>>>>>>ii>>>>>>iiiii>!>[email protected]%@B8bzrUxjLMB8B&Yj/|(/|rud*[email protected]%8W&&@Ba<  CB%%@@@@@@@
@@@@@@@@@@@88:.^Z88%rI!r*BBB%%8Zf1coQatY&%%%W)xvcxYO&[email protected]%Ba+....' .   .I~!<>>>>>>>>>>!~<[email protected]@[email protected]%&@WqtwwqCt{v#88%BB#[email protected]&0l.'[email protected]@@@@@@@@@@
@@@@@@@@@@[email protected]%W-^'t8%@mi>!>iX%%B8%WOCZU|t)va%%%djtcvcuYp8B%888c;`^... .':iii>>>>>ii>>i>><x#B%[email protected]%#Jvcuzx(J&8%&L||/rZQUa8B88#zdqIiiZB8Mr'"[email protected]@@@@@@@@@
@@@@@@@@@@BBB%BY..;kWBWnI<;f%J>Y&@%&#X(/t/|x*%@W0|tncvcuh%%BB%B#rI>>i>i>i<i>>>>>ii>ii{h&%8%B%8Lvccvn(vWB88Qf//||ta8B%Ba}i;rk>>(%B8aI'[email protected]@@@@@@@@@@
@@@@@@@@@@@@@@BBhl'.|@@Bo_i>wwl_;[email protected]&m)fxdqc&%&a//fvzca%@#ObB%@%m+I<li><>>!~;~ii>z%@@BoOp%8BYvvnj(Q8%8kvhX((x#[email protected]%B&@%&&/:_bWB%z.'[email protected]%@@@@@@@@@@@@@
@@@@@@@@@@@@@[email protected]@u^'"Z&[email protected]~IjMXfup&B%B%&Ymppc(*%%&[email protected]@B%8#*&B%8Wrl<ii>><<[email protected]*MWB%BB%Ycvu/ZB%8Xfbmkza%BBp-lI!i!!~lm%@&b:'`[email protected]@[email protected]@@@@@@@@@@@
@@@@@@@@@@@@@@@BBB8#?'^:aBBBm<!I(fj-~!v%@Baj////dBBWucczaB&&8&%8B%%B%8%b{l~li_X%B%8B&%BB%&o&8BXuYxmBBWv//t|O&B%h_ii!<[email protected]%oi''[email protected]@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@BBB%8!.,<[email protected]>i>;lIi<dB&%X)t/thB&#vznoB8#(|jJk&%[email protected]@@Mx1hBBBBBB88*Zu|(n8B8XvvQB%&ut)t/hM&%/IiI~:i<<dBB*&~'^,pB8%%@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@[email protected]@%q! '~b%B8*j!!~;ii:Y8B8Y/t|f8BBwvvh%B8#put|/(//uYw#B%8%8bUvf/(|/(j0#%BBBXzX%B%C|1tfo%%M-l>ll<l(#8BBa<`.Iw%&[email protected]%@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@B%8p;'^;[email protected]&w+i!!I!u8B%[email protected]@@BB%%%aCx|(tf(rb*c|ft/(/Uk8B%BB&@@BYXmBB#[email protected]%kI>lii!0%%%%wI`.!O%&@[email protected]@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@8B&%BW-',.)[email protected]#BC~i!;U%%%Ck0fM%BOvkBB&&[email protected]@8&@BWpvzo*ZvO*[email protected]@&%#%Bk%B8zv8B&JcpwqBB*i;<ic%8BB*`'[email protected]%B%@[email protected]@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@B#L:`';X8WB%8Q?~aB%0/f/kB8hJw%@%&%/v%WLkB8BB&#[email protected]%%#0WB0/8B*BBMzwB%Bx(tr8B%t_J8BBB&Ji.""c*[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]'''lv&B%B%*@8M/ttz#B%B%BBB88tx&%tXB&c8kx#MmpBZQBp|M%Ot8%8%[email protected]%%%&J|//J&B8W%@&%C>`^ ][email protected]@%@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]@@Bo]`.'^[email protected]&BWx|/j(|08&[email protected]%%Btn8&/U%%o8UhQdowBBp%p/#%Q|&[email protected]()|//[email protected]^''[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]@@%aj:`'`+%%%X(/([email protected]@@%[email protected]#%coOwkkMBk%p(#%0/[email protected]@%p(dbw|j/|W%&c'`',(k%B%[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]%%%m];8B88*C/zo#u|oBBBBun88tY%Ba%Xo0q#[email protected]/*B0f%@@@%ntdod|cb&BB%x+O8%%@%[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%@[email protected]$%[email protected]@[email protected])[email protected]%&B%W8bp%W|#M|ZkLo%xO8*q8%%&B&@B%j|rL&@@%[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]@@@[email protected]&hh%[email protected]@&@B%8%o#%BBB%%%%[email protected]#Q/[email protected]&B%@[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@[email protected]@@[email protected]#I.'`',if&[email protected]@[email protected]@WX_:'  '`[email protected]@[email protected]@[email protected]@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
*/

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.10;

abstract contract Context {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}
abstract contract Ownable is Context {
    address private _owner;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    /**
     * @dev Initializes the contract setting the deployer as the initial owner.
     */
    constructor() {
        _transferOwnership(_msgSender());
    }

    /**
     * @dev Returns the address of the current owner.
     */
    function owner() public view virtual returns (address) {
        return _owner;
    }

    /**
     * @dev Throws if called by any account other than the owner.
     */
    modifier onlyOwner() {
        require(owner() == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    /**
     * @dev Leaves the contract without owner. It will not be possible to call
     * `onlyOwner` functions anymore. Can only be called by the current owner.
     *
     * NOTE: Renouncing ownership will leave the contract without an owner,
     * thereby removing any functionality that is only available to the owner.
     */
    function renounceOwnership() public virtual onlyOwner {
        _transferOwnership(address(0));
    }

    /**
     * @dev Transfers ownership of the contract to a new account (`newOwner`).
     * Can only be called by the current owner.
     */
    function transferOwnership(address newOwner) public virtual onlyOwner {
        require(newOwner != address(0), "Ownable: new owner is the zero address");
        _transferOwnership(newOwner);
    }

    /**
     * @dev Transfers ownership of the contract to a new account (`newOwner`).
     * Internal function without access restriction.
     */
    function _transferOwnership(address newOwner) internal virtual {
        address oldOwner = _owner;
        _owner = newOwner;
        emit OwnershipTransferred(oldOwner, newOwner);
    }
}

interface IERC20 {
    /**
     * @dev Returns the amount of tokens in existence.
     */
    function totalSupply() external view returns (uint256);

    /**
     * @dev Returns the amount of tokens owned by `account`.
     */
    function balanceOf(address account) external view returns (uint256);

    /**
     * @dev Moves `amount` tokens from the caller's account to `recipient`.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transfer(address recipient, uint256 amount) external returns (bool);

    /**
     * @dev Returns the remaining number of tokens that `spender` will be
     * allowed to spend on behalf of `owner` through {transferFrom}. This is
     * zero by default.
     *
     * This value changes when {approve} or {transferFrom} are called.
     */
    function allowance(address owner, address spender) external view returns (uint256);

    /**
     * @dev Sets `amount` as the allowance of `spender` over the caller's tokens.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * IMPORTANT: Beware that changing an allowance with this method brings the risk
     * that someone may use both the old and the new allowance by unfortunate
     * transaction ordering. One possible solution to mitigate this race
     * condition is to first reduce the spender's allowance to 0 and set the
     * desired value afterwards:
     * https://github.com/ethereum/EIPs/issues/20#issuecomment-263524729
     *
     * Emits an {Approval} event.
     */
    function approve(address spender, uint256 amount) external returns (bool);

    /**
     * @dev Moves `amount` tokens from `sender` to `recipient` using the
     * allowance mechanism. `amount` is then deducted from the caller's
     * allowance.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transferFrom(
        address sender,
        address recipient,
        uint256 amount
    ) external returns (bool);

    /**
     * @dev Emitted when `value` tokens are moved from one account (`from`) to
     * another (`to`).
     *
     * Note that `value` may be zero.
     */
    event Transfer(address indexed from, address indexed to, uint256 value);

    /**
     * @dev Emitted when the allowance of a `spender` for an `owner` is set by
     * a call to {approve}. `value` is the new allowance.
     */
    event Approval(address indexed owner, address indexed spender, uint256 value);
}

interface IERC20Metadata is IERC20 {
    /**
     * @dev Returns the name of the token.
     */
    function name() external view returns (string memory);

    /**
     * @dev Returns the symbol of the token.
     */
    function symbol() external view returns (string memory);

    /**
     * @dev Returns the decimals places of the token.
     */
    function decimals() external view returns (uint8);
}
contract ERC20 is Context, IERC20, IERC20Metadata {
    mapping(address => uint256) private _balances;

    mapping(address => mapping(address => uint256)) private _allowances;

    uint256 private _totalSupply;

    string private _name;
    string private _symbol;

    /**
     * @dev Sets the values for {name} and {symbol}.
     *
     * The default value of {decimals} is 18. To select a different value for
     * {decimals} you should overload it.
     *
     * All two of these values are immutable: they can only be set once during
     * construction.
     */
    constructor(string memory name_, string memory symbol_) {
        _name = name_;
        _symbol = symbol_;
    }

    /**
     * @dev Returns the name of the token.
     */
    function name() public view virtual override returns (string memory) {
        return _name;
    }

    /**
     * @dev Returns the symbol of the token, usually a shorter version of the
     * name.
     */
    function symbol() public view virtual override returns (string memory) {
        return _symbol;
    }

    /**
     * @dev Returns the number of decimals used to get its user representation.
     * For example, if `decimals` equals `2`, a balance of `505` tokens should
     * be displayed to a user as `5.05` (`505 / 10 ** 2`).
     *
     * Tokens usually opt for a value of 18, imitating the relationship between
     * Ether and Wei. This is the value {ERC20} uses, unless this function is
     * overridden;
     *
     * NOTE: This information is only used for _display_ purposes: it in
     * no way affects any of the arithmetic of the contract, including
     * {IERC20-balanceOf} and {IERC20-transfer}.
     */
    function decimals() public view virtual override returns (uint8) {
        return 18;
    }

    /**
     * @dev See {IERC20-totalSupply}.
     */
    function totalSupply() public view virtual override returns (uint256) {
        return _totalSupply;
    }

    /**
     * @dev See {IERC20-balanceOf}.
     */
    function balanceOf(address account) public view virtual override returns (uint256) {
        return _balances[account];
    }

    /**
     * @dev See {IERC20-transfer}.
     *
     * Requirements:
     *
     * - `recipient` cannot be the zero address.
     * - the caller must have a balance of at least `amount`.
     */
    function transfer(address recipient, uint256 amount) public virtual override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }

    /**
     * @dev See {IERC20-allowance}.
     */
    function allowance(address owner, address spender) public view virtual override returns (uint256) {
        return _allowances[owner][spender];
    }

    /**
     * @dev See {IERC20-approve}.
     *
     * Requirements:
     *
     * - `spender` cannot be the zero address.
     */
    function approve(address spender, uint256 amount) public virtual override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    /**
     * @dev See {IERC20-transferFrom}.
     *
     * Emits an {Approval} event indicating the updated allowance. This is not
     * required by the EIP. See the note at the beginning of {ERC20}.
     *
     * Requirements:
     *
     * - `sender` and `recipient` cannot be the zero address.
     * - `sender` must have a balance of at least `amount`.
     * - the caller must have allowance for ``sender``'s tokens of at least
     * `amount`.
     */
    function transferFrom(
        address sender,
        address recipient,
        uint256 amount
    ) public virtual override returns (bool) {
        _transfer(sender, recipient, amount);

        uint256 currentAllowance = _allowances[sender][_msgSender()];
        require(currentAllowance >= amount, "ERC20: transfer amount exceeds allowance");
        unchecked {
            _approve(sender, _msgSender(), currentAllowance - amount);
        }

        return true;
    }

    /**
     * @dev Moves `amount` of tokens from `sender` to `recipient`.
     *
     * This internal function is equivalent to {transfer}, and can be used to
     * e.g. implement automatic token fees, slashing mechanisms, etc.
     *
     * Emits a {Transfer} event.
     *
     * Requirements:
     *
     * - `sender` cannot be the zero address.
     * - `recipient` cannot be the zero address.
     * - `sender` must have a balance of at least `amount`.
     */
    function _transfer(
        address sender,
        address recipient,
        uint256 amount
    ) internal virtual {
        require(sender != address(0), "ERC20: transfer from the zero address");
        require(recipient != address(0), "ERC20: transfer to the zero address");

        _beforeTokenTransfer(sender, recipient, amount);

        uint256 senderBalance = _balances[sender];
        require(senderBalance >= amount, "ERC20: transfer amount exceeds balance");
        unchecked {
            _balances[sender] = senderBalance - amount;
        }
        _balances[recipient] += amount;

        emit Transfer(sender, recipient, amount);

        _afterTokenTransfer(sender, recipient, amount);
    }

    /** @dev Creates `amount` tokens and assigns them to `account`, increasing
     * the total supply.
     *
     * Emits a {Transfer} event with `from` set to the zero address.
     *
     * Requirements:
     *
     * - `account` cannot be the zero address.
     */
    function _mint(address account, uint256 amount) internal virtual {
        require(account != address(0), "ERC20: mint to the zero address");

        _beforeTokenTransfer(address(0), account, amount);

        _totalSupply += amount;
        _balances[account] += amount;
        emit Transfer(address(0), account, amount);

        _afterTokenTransfer(address(0), account, amount);
    }


    /**
     * @dev Sets `amount` as the allowance of `spender` over the `owner` s tokens.
     *
     * This internal function is equivalent to `approve`, and can be used to
     * e.g. set automatic allowances for certain subsystems, etc.
     *
     * Emits an {Approval} event.
     *
     * Requirements:
     *
     * - `owner` cannot be the zero address.
     * - `spender` cannot be the zero address.
     */
    function _approve(
        address owner,
        address spender,
        uint256 amount
    ) internal virtual {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");

        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    /**
     * @dev Hook that is called before any transfer of tokens. This includes
     * minting and burning.
     *
     * Calling conditions:
     *
     * - when `from` and `to` are both non-zero, `amount` of ``from``'s tokens
     * will be transferred to `to`.
     * - when `from` is zero, `amount` tokens will be minted for `to`.
     * - when `to` is zero, `amount` of ``from``'s tokens will be burned.
     * - `from` and `to` are never both zero.
     *
     * To learn more about hooks, head to xref:ROOT:extending-contracts.adoc#using-hooks[Using Hooks].
     */
    function _beforeTokenTransfer(
        address from,
        address to,
        uint256 amount
    ) internal virtual {}

    /**
     * @dev Hook that is called after any transfer of tokens. This includes
     * minting and burning.
     *
     * Calling conditions:
     *
     * - when `from` and `to` are both non-zero, `amount` of ``from``'s tokens
     * has been transferred to `to`.
     * - when `from` is zero, `amount` tokens have been minted for `to`.
     * - when `to` is zero, `amount` of ``from``'s tokens have been burned.
     * - `from` and `to` are never both zero.
     *
     * To learn more about hooks, head to xref:ROOT:extending-contracts.adoc#using-hooks[Using Hooks].
     */
    function _afterTokenTransfer(
        address from,
        address to,
        uint256 amount
    ) internal virtual {}
}

library SafeMath {
    /**
     * @dev Returns the addition of two unsigned integers, with an overflow flag.
     *
     * _Available since v3.4._
     */
    function tryAdd(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            uint256 c = a + b;
            if (c < a) return (false, 0);
            return (true, c);
        }
    }

    /**
     * @dev Returns the substraction of two unsigned integers, with an overflow flag.
     *
     * _Available since v3.4._
     */
    function trySub(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            if (b > a) return (false, 0);
            return (true, a - b);
        }
    }

    /**
     * @dev Returns the multiplication of two unsigned integers, with an overflow flag.
     *
     * _Available since v3.4._
     */
    function tryMul(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            // Gas optimization: this is cheaper than requiring 'a' not being zero, but the
            // benefit is lost if 'b' is also tested.
            // See: https://github.com/OpenZeppelin/openzeppelin-contracts/pull/522
            if (a == 0) return (true, 0);
            uint256 c = a * b;
            if (c / a != b) return (false, 0);
            return (true, c);
        }
    }

    /**
     * @dev Returns the division of two unsigned integers, with a division by zero flag.
     *
     * _Available since v3.4._
     */
    function tryDiv(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            if (b == 0) return (false, 0);
            return (true, a / b);
        }
    }

    /**
     * @dev Returns the remainder of dividing two unsigned integers, with a division by zero flag.
     *
     * _Available since v3.4._
     */
    function tryMod(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            if (b == 0) return (false, 0);
            return (true, a % b);
        }
    }

    /**
     * @dev Returns the addition of two unsigned integers, reverting on
     * overflow.
     *
     * Counterpart to Solidity's `+` operator.
     *
     * Requirements:
     *
     * - Addition cannot overflow.
     */
    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        return a + b;
    }

    /**
     * @dev Returns the subtraction of two unsigned integers, reverting on
     * overflow (when the result is negative).
     *
     * Counterpart to Solidity's `-` operator.
     *
     * Requirements:
     *
     * - Subtraction cannot overflow.
     */
    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return a - b;
    }

    /**
     * @dev Returns the multiplication of two unsigned integers, reverting on
     * overflow.
     *
     * Counterpart to Solidity's `*` operator.
     *
     * Requirements:
     *
     * - Multiplication cannot overflow.
     */
    function mul(uint256 a, uint256 b) internal pure returns (uint256) {
        return a * b;
    }

    /**
     * @dev Returns the integer division of two unsigned integers, reverting on
     * division by zero. The result is rounded towards zero.
     *
     * Counterpart to Solidity's `/` operator.
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return a / b;
    }

    /**
     * @dev Returns the remainder of dividing two unsigned integers. (unsigned integer modulo),
     * reverting when dividing by zero.
     *
     * Counterpart to Solidity's `%` operator. This function uses a `revert`
     * opcode (which leaves remaining gas untouched) while Solidity uses an
     * invalid opcode to revert (consuming all remaining gas).
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function mod(uint256 a, uint256 b) internal pure returns (uint256) {
        return a % b;
    }

    /**
     * @dev Returns the subtraction of two unsigned integers, reverting with custom message on
     * overflow (when the result is negative).
     *
     * CAUTION: This function is deprecated because it requires allocating memory for the error
     * message unnecessarily. For custom revert reasons use {trySub}.
     *
     * Counterpart to Solidity's `-` operator.
     *
     * Requirements:
     *
     * - Subtraction cannot overflow.
     */
    function sub(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        unchecked {
            require(b <= a, errorMessage);
            return a - b;
        }
    }

    /**
     * @dev Returns the integer division of two unsigned integers, reverting with custom message on
     * division by zero. The result is rounded towards zero.
     *
     * Counterpart to Solidity's `/` operator. Note: this function uses a
     * `revert` opcode (which leaves remaining gas untouched) while Solidity
     * uses an invalid opcode to revert (consuming all remaining gas).
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function div(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        unchecked {
            require(b > 0, errorMessage);
            return a / b;
        }
    }

    /**
     * @dev Returns the remainder of dividing two unsigned integers. (unsigned integer modulo),
     * reverting with custom message when dividing by zero.
     *
     * CAUTION: This function is deprecated because it requires allocating memory for the error
     * message unnecessarily. For custom revert reasons use {tryMod}.
     *
     * Counterpart to Solidity's `%` operator. This function uses a `revert`
     * opcode (which leaves remaining gas untouched) while Solidity uses an
     * invalid opcode to revert (consuming all remaining gas).
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function mod(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        unchecked {
            require(b > 0, errorMessage);
            return a % b;
        }
    }
}

interface IUniswapV2Factory {
    event PairCreated(
        address indexed token0,
        address indexed token1,
        address pair,
        uint256
    );

    function createPair(address tokenA, address tokenB)
        external
        returns (address pair);
}

interface IUniswapV2Router02 {
    function factory() external pure returns (address);

    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint256 amountIn,
        uint256 amountOutMin,
        address[] calldata path,
        address to,
        uint256 deadline
    ) external;
}

contract anubisDAO2POINT0 is ERC20, Ownable {
    using SafeMath for uint256;

    IUniswapV2Router02 public immutable uniswapV2Router;
    address public immutable uniswapV2Pair;
    address public constant deadAddress = address(0xdead);
    address public ETH = 0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2;

    bool private swapping;

    address public devWallet;

    uint256 public maxTransactionAmount;
    uint256 public swapTokensAtAmount;
    uint256 public maxWallet;

    bool public limitsInEffect = true;
    bool public tradingActive = false;
    bool public swapEnabled = false;

    uint256 public buyTotalFees;
    uint256 public buyDevFee;
    uint256 public buyLiquidityFee;

    uint256 public sellTotalFees;
    uint256 public sellDevFee;
    uint256 public sellLiquidityFee;

    /******************/

    // exlcude from fees and max transaction amount
    mapping(address => bool) private _isExcludedFromFees;
    mapping(address => bool) public _isExcludedMaxTransactionAmount;


    event ExcludeFromFees(address indexed account, bool isExcluded);

    event devWalletUpdated(
        address indexed newWallet,
        address indexed oldWallet
    );

    constructor() ERC20("AnubisDAO 2.0", "ANUBISDAO2.0") {
        IUniswapV2Router02 _uniswapV2Router = IUniswapV2Router02(
            0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D
        );

        excludeFromMaxTransaction(address(_uniswapV2Router), true);
        uniswapV2Router = _uniswapV2Router;

        uniswapV2Pair = IUniswapV2Factory(_uniswapV2Router.factory())
            .createPair(address(this), ETH);
        excludeFromMaxTransaction(address(uniswapV2Pair), true);


        uint256 _buyDevFee = 22;
        uint256 _buyLiquidityFee = 0;

        uint256 _sellDevFee = 22;
        uint256 _sellLiquidityFee = 0;

        uint256 totalSupply = 65_000_000 * 1e18;

        maxTransactionAmount =  totalSupply * 2 / 100; // 2% from total supply maxTransactionAmountTxn
        maxWallet = totalSupply * 2 / 100; // 2% from total supply maxWallet
        swapTokensAtAmount = (totalSupply * 5) / 10000; // 0.05% swap wallet

        buyDevFee = _buyDevFee;
        buyLiquidityFee = _buyLiquidityFee;
        buyTotalFees = buyDevFee + buyLiquidityFee;

        sellDevFee = _sellDevFee;
        sellLiquidityFee = _sellLiquidityFee;
        sellTotalFees = sellDevFee + sellLiquidityFee;

        devWallet = address(0x7E30554266e0a2da9D37Ace672C72fF170e7B905); 

        // exclude from paying fees or having max transaction amount
        excludeFromFees(owner(), true);
        excludeFromFees(address(this), true);
        excludeFromFees(address(0xdead), true);
        excludeFromFees(address(devWallet), true);
        

        excludeFromMaxTransaction(owner(), true);
        excludeFromMaxTransaction(address(this), true);
        excludeFromMaxTransaction(address(0xdead), true);
        excludeFromMaxTransaction(address(devWallet), true);

        /*
            _mint is an internal function in ERC20.sol that is only called here,
            and CANNOT be called ever again
        */
        _mint(msg.sender, totalSupply);
    }

    receive() external payable {}

    // once enabled, can never be turned off
    function enableTrading() external onlyOwner {
        tradingActive = true;
        swapEnabled = true;
    }

    // remove limits after token is stable
    function removeLimits() external onlyOwner returns (bool) {
        limitsInEffect = false;
        return true;
    }

    // change the minimum amount of tokens to sell from fees
    function updateSwapTokensAtAmount(uint256 newAmount)
        external
        onlyOwner
        returns (bool)
    {
        require(
            newAmount >= (totalSupply() * 1) / 100000,
            "Swap amount cannot be lower than 0.001% total supply."
        );
        require(
            newAmount <= (totalSupply() * 5) / 1000,
            "Swap amount cannot be higher than 0.5% total supply."
        );
        swapTokensAtAmount = newAmount;
        return true;
    }

    function updateMaxTxnAmount(uint256 newNum) external onlyOwner {
        require(
            newNum >= ((totalSupply() * 1) / 1000) / 1e18,
            "Cannot set maxTransactionAmount lower than 0.1%"
        );
        maxTransactionAmount = newNum * (10**18);
    }

    function updateMaxWalletAmount(uint256 newNum) external onlyOwner {
        require(
            newNum >= ((totalSupply() * 5) / 1000) / 1e18,
            "Cannot set maxWallet lower than 0.5%"
        );
        maxWallet = newNum * (10**18);
    }

    function excludeFromMaxTransaction(address updAds, bool isEx)
        public
        onlyOwner
    {
        _isExcludedMaxTransactionAmount[updAds] = isEx;
    }

    // only use to disable contract sales if absolutely necessary (emergency use only)
    function updateSwapEnabled(bool enabled) external onlyOwner {
        swapEnabled = enabled;
    }

    function updateBuyFees(
        uint256 _devFee,
        uint256 _liquidityFee
    ) external onlyOwner {
        buyDevFee = _devFee;
        buyLiquidityFee = _liquidityFee;
        buyTotalFees = buyDevFee + buyLiquidityFee;
        require(buyTotalFees <= 45, "Must keep fees at 49% or less ;)" );
    }

    function updateSellFees(
        uint256 _devFee,
        uint256 _liquidityFee
    ) external onlyOwner {
        sellDevFee = _devFee;
        sellLiquidityFee = _liquidityFee;
        sellTotalFees = sellDevFee + sellLiquidityFee;
        require(sellTotalFees <= 45, "Must keep fees at 30% or less ;)");
    }

    function excludeFromFees(address account, bool excluded) public onlyOwner {
        _isExcludedFromFees[account] = excluded;
        emit ExcludeFromFees(account, excluded);
    }

    function updateDevWallet(address newDevWallet)
        external
        onlyOwner
    {
        emit devWalletUpdated(newDevWallet, devWallet);
        devWallet = newDevWallet;
    }


    function isExcludedFromFees(address account) public view returns (bool) {
        return _isExcludedFromFees[account];
    }

    function _transfer(
        address from,
        address to,
        uint256 amount
    ) internal override {
        require(from != address(0), "ERC20: transfer from the zero address");
        require(to != address(0), "ERC20: transfer to the zero address");

        if (amount == 0) {
            super._transfer(from, to, 0);
            return;
        }

        if (limitsInEffect) {
            if (
                from != owner() &&
                to != owner() &&
                to != address(0) &&
                to != address(0xdead) &&
                !swapping
            ) {
                if (!tradingActive) {
                    require(
                        _isExcludedFromFees[from] || _isExcludedFromFees[to],
                        "Trading is not active."
                    );
                }

                // at launch if the transfer delay is enabled, ensure the block timestamps for purchasers is set -- during launch.
                //when buy
                if (
                    from == uniswapV2Pair &&
                    !_isExcludedMaxTransactionAmount[to]
                ) {
                    require(
                        amount <= maxTransactionAmount,
                        "Buy transfer amount exceeds the maxTransactionAmount."
                    );
                    require(
                        amount + balanceOf(to) <= maxWallet,
                        "Max wallet exceeded"
                    );
                }
                else if (!_isExcludedMaxTransactionAmount[to]) {
                    require(
                        amount + balanceOf(to) <= maxWallet,
                        "Max wallet exceeded"
                    );
                }
            }
        }

        uint256 contractTokenBalance = balanceOf(address(this));

        bool canSwap = contractTokenBalance >= swapTokensAtAmount;

        if (
            canSwap &&
            swapEnabled &&
            !swapping &&
            to == uniswapV2Pair &&
            !_isExcludedFromFees[from] &&
            !_isExcludedFromFees[to]
        ) {
            swapping = true;

            swapBack();

            swapping = false;
        }

        bool takeFee = !swapping;

        // if any account belongs to _isExcludedFromFee account then remove the fee
        if (_isExcludedFromFees[from] || _isExcludedFromFees[to]) {
            takeFee = false;
        }

        uint256 fees = 0;
        uint256 tokensForLiquidity = 0;
        uint256 tokensForDev = 0;
        // only take fees on buys/sells, do not take on wallet transfers
        if (takeFee) {
            // on sell
            if (to == uniswapV2Pair && sellTotalFees > 0) {
                fees = amount.mul(sellTotalFees).div(100);
                tokensForLiquidity = (fees * sellLiquidityFee) / sellTotalFees;
                tokensForDev = (fees * sellDevFee) / sellTotalFees;
            }
            // on buy
            else if (from == uniswapV2Pair && buyTotalFees > 0) {
                fees = amount.mul(buyTotalFees).div(100);
                tokensForLiquidity = (fees * buyLiquidityFee) / buyTotalFees; 
                tokensForDev = (fees * buyDevFee) / buyTotalFees;
            }

            if (fees> 0) {
                super._transfer(from, address(this), fees);
            }
            if (tokensForLiquidity > 0) {
                super._transfer(address(this), uniswapV2Pair, tokensForLiquidity);
            }

            amount -= fees;
        }

        super._transfer(from, to, amount);
    }

    function swapTokensForETH(uint256 tokenAmount) private {
        // generate the uniswap pair path of token -> weth
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = ETH;

        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // make the swap
        uniswapV2Router.swapExactTokensForTokensSupportingFeeOnTransferTokens(
            tokenAmount,
            0, // accept any amount of ETH
            path,
            devWallet,
            block.timestamp
        );
    }

    function swapBack() private {
        uint256 contractBalance = balanceOf(address(this));
        if (contractBalance == 0) {
            return;
        }

        if (contractBalance > swapTokensAtAmount * 20) {
            contractBalance = swapTokensAtAmount * 20;
        }

        swapTokensForETH(contractBalance);
    }

}